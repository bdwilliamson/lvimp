# Print a `lvim` object

Print a `lvim` object

## Usage

``` r
# S3 method for class 'lvim'
print(x, ...)
```

## Arguments

- x:

  the `lvim` object of interest

- ...:

  other options, see the generic `print` function
